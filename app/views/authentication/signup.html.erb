 <%= form_tag(books_path, :method => "get", class: "navbar-form", id: "search-form") do %>
    <div class="input-append">
      <%= text_field_tag :name, params[:name], class: "span2", placeholder: "Username" %>
      <%= text_field_tag :email, params[:email], class: "span2", placeholder: "Email" %>
      <%= password_field_tag :password, params[:nil], class: "span2"%>

      <!-- In order to have the "search" icon int the button, we need to use plain HTML instead 
           of using a Rails form helper -->
      <button class="btn" type="submit"><i class="icon-search"></i></button>
    </div>
    <%= link_to 'Back', books_path %>
  <% end %>